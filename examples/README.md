# Examples

All examples are synthetic and designed for repeatable public workflows.

Current examples:

- `application-packet.example.json`
- `status-event.example.json`
- `document-request.example.json`
- `form.example.json`
- `notice.example.json`

Every example validates against its schema in CI (`npm run validate`).
