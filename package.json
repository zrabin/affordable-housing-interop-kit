{
  "name": "open-housing-data-standard",
  "version": "0.1.0",
  "description": "Open Housing Data Standard: an open data standard for New York's affordable housing applications, with a reference implementation.",
  "license": "MIT",
  "author": "Zach Rabin",
  "type": "module",
  "bin": { "ahik": "cli/ahik.mjs" },
  "scripts": {
    "validate:json": "node scripts/validate-json.mjs",
    "validate:schema": "node scripts/validate-schema.mjs",
    "validate": "npm run validate:json && npm run validate:schema",
    "generate": "node synthetic-data/generate.mjs",
    "demo": "node cli/ahik.mjs demo",
    "mcp": "node cli/ahik.mjs mcp",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
